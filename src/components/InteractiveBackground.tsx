import { useEffect, useRef } from 'react';

const InteractiveBackground = () => {
  // Remove all blurred blobs for a clean, tech-inspired look
  return null;
};

export default InteractiveBackground; 